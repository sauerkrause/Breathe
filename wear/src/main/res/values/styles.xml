<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.Breathe" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="colorAccent">@color/light_accent</item>
    </style>

    <style name="Theme.Breathe" parent="Base.Theme.Breathe" />

    <style name="ShapeAppearanceOverlay.Breathe.Button.Round" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>